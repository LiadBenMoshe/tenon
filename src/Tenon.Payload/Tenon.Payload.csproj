<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>netstandard2.0;net8.0</TargetFrameworks>
    <RootNamespace>Tenon.Payload</RootNamespace>
    <Description>Tenon Setup.exe payload container format and runtime manifest, shared by the CLI (writer) and the setup stub (reader).</Description>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="System.Text.Json" />
  </ItemGroup>
</Project>
