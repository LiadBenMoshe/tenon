**What changed and why**

**How it was tested**
- [ ] `dotnet test Tenon.sln`
- [ ] `tests/e2e/run.ps1` (when the generated installer changes)
- [ ] Docs and `schema/tenon.schema.json` updated (when `tenon.json` changes)

**Windows Installer rules touched** (component key paths, per-user folders, sequences), if any:
